using System;
using System.Collections.Generic;
using System.Globalization;
using System.Linq;
using System.Text;
using System.Text.RegularExpressions;
using System.Threading.Tasks;
using static System.Windows.Forms.VisualStyles.VisualStyleElement;

namespace Mush.src.Application.Ports
{
    internal interface IProjectService
    {
        
            //Task<IReadOnlyList<MycoProject>> GetProjectsAsync();
            //Task<IReadOnlyList<Culture>> GetCulturesAsync(Guid projectId);
            //Task<IReadOnlyList<Spawn>> GetSpawnsForCultureAsync(Guid cultureId);

            //Task AddCultureAsync(Guid projectId, CultureType type, string notes);
            //Task AddSpawnAsync(Guid cultureId, DateTime inoculatedAt, string grain, string? notes);

            //Task SaveAsync();
        
    }
}
